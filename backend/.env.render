# ⚙️ Variáveis de Ambiente para o Render
# Copie estas variáveis para o painel do Render

# MongoDB Atlas - OBRIGATÓRIO
MONGODB_URI=mongodb+srv://wagnermocelin_db_user:iSCEoUtchmw5cI3A@area52.yreusoc.mongodb.net/area52?retryWrites=true&w=majority&appName=area52

# Node Environment - OBRIGATÓRIO
NODE_ENV=production

# Porta - OBRIGATÓRIO (Render define automaticamente, mas pode especificar)
PORT=5000

# JWT Configuration - OBRIGATÓRIO
JWT_SECRET=area52_secret_key_2024_change_in_production
JWT_EXPIRE=30d

# CORS - OPCIONAL (Configure com a URL do seu frontend)
# Exemplo: https://seu-frontend.netlify.app
CORS_ORIGIN=*

# Frontend URL - OPCIONAL (Para links em emails)
# Exemplo: https://seu-frontend.netlify.app
FRONTEND_URL=http://localhost:3000

# ⚠️ IMPORTANTE:
# 1. Não commite este arquivo no Git (já está no .gitignore)
# 2. Configure estas variáveis manualmente no painel do Render
# 3. Após configurar, faça redeploy
